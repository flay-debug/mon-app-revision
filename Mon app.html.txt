<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="theme-color" content="#3B82F6">
<title>Mon App de Révision</title>
<script crossorigin src="https://cdnjs.cloudflare.com/ajax/libs/react/18.2.0/umd/react.production.min.js"></script>
<script crossorigin src="https://cdnjs.cloudflare.com/ajax/libs/react-dom/18.2.0/umd/react-dom.production.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/babel-standalone/7.23.5/babel.min.js"></script>
<script src="https://cdn.tailwindcss.com"></script>
<style>body{margin:0;padding:0;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif}*{box-sizing:border-box}</style>
</head>
<body>
<div id="root"></div>
<script type="text/babel">
const {useState,useEffect}=React;const App=()=>{const[tab,setTab]=useState('calendar');const[notes,setNotes]=useState(()=>JSON.parse(localStorage.getItem('notes')||'{}'));const[calendar,setCalendar]=useState(()=>JSON.parse(localStorage.getItem('calendar')||'{}'));const[tasks,setTasks]=useState(()=>JSON.parse(localStorage.getItem('tasks')||'[]'));const[reminders,setReminders]=useState(()=>JSON.parse(localStorage.getItem('reminders')||'[]'));const[subjects,setSubjects]=useState(()=>JSON.parse(localStorage.getItem('subjects')||'["Mathématiques","Français","Physique","Chimie","Histoire","Anglais"]'));const[activityTypes,setActivityTypes]=useState(()=>JSON.parse(localStorage.getItem('activityTypes')||'["Quiz","Exercice","Annales","Lecture","Évaluation"]'));const[isPlaying,setIsPlaying]=useState(false);const[audio,setAudio]=useState(null);const emojis=['😊','😐','😕','💪','🌟','✅'];const today=new Date().toISOString().split('T')[0];useEffect(()=>{localStorage.setItem('notes',JSON.stringify(notes))},[notes]);useEffect(()=>{localStorage.setItem('calendar',JSON.stringify(calendar))},[calendar]);useEffect(()=>{localStorage.setItem('tasks',JSON.stringify(tasks))},[tasks]);useEffect(()=>{localStorage.setItem('reminders',JSON.stringify(reminders))},[reminders]);useEffect(()=>{localStorage.setItem('subjects',JSON.stringify(subjects))},[subjects]);useEffect(()=>{localStorage.setItem('activityTypes',JSON.stringify(activityTypes))},[activityTypes]);const CalendarView=()=>{const[date,setDate]=useState(today);const[subject,setSubject]=useState('');const[activities,setActivities]=useState([]);const[emoji,setEmoji]=useState('');const save=()=>{if(subject&&activities.length>0){if(!subjects.includes(subject))setSubjects([...subjects,subject]);setCalendar({...calendar,[date]:{...calendar[date],[subject]:{activities,emoji}}});setSubject('');setActivities([]);setEmoji('')}};return(<div className="space-y-4"><div className="bg-white rounded-lg shadow p-4"><input type="date"value={date}onChange={e=>setDate(e.target.value)}className="w-full p-3 border-2 rounded-lg mb-4"/><input type="text"list="s-list"value={subject}onChange={e=>setSubject(e.target.value)}placeholder="Matière"className="w-full p-3 border-2 rounded-lg mb-4"/><datalist id="s-list">{subjects.map(s=><option key={s}value={s}/>)}</datalist><div className="grid grid-cols-2 gap-2 mb-4">{activityTypes.map(t=>(<button key={t}onClick={()=>setActivities(prev=>prev.includes(t)?prev.filter(a=>a!==t):[...prev,t])}className={`p-3 rounded-lg font-semibold ${activities.includes(t)?'bg-blue-500 text-white':'bg-gray-100'}`}>{t}</button>))}</div><div className="flex gap-2 justify-center mb-4 flex-wrap">{emojis.map(e=>(<button key={e}onClick={()=>setEmoji(e)}className={`text-4xl p-2 rounded ${emoji===e?'bg-yellow-100':''}`}>{e}</button>))}</div><button onClick={save}className="w-full bg-blue-500 text-white p-3 rounded-lg font-bold">Enregistrer</button></div><div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">Résumé</h3>{calendar[date]?Object.entries(calendar[date]).map(([s,d])=>(<div key={s}className="border-l-4 border-blue-500 pl-3 py-2 mb-2"><p className="font-semibold">{s} {d.emoji}</p><p className="text-sm text-gray-600">{d.activities.join(', ')}</p></div>)):<p className="text-gray-500">Aucune activité</p>}</div></div>)};const NotesView=()=>{const[subject,setSubject]=useState('');const[ex,setEx]=useState('');const[score,setScore]=useState('');const[emoji,setEmoji]=useState('');const add=()=>{if(subject&&ex){if(!subjects.includes(subject))setSubjects([...subjects,subject]);setNotes({...notes,[Date.now()]:{subject,exerciseName:ex,score,emoji,date:today}});setSubject('');setEx('');setScore('');setEmoji('')}};return(<div className="space-y-4"><div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">Ajouter</h3><input type="text"list="s-notes"value={subject}onChange={e=>setSubject(e.target.value)}placeholder="Matière"className="w-full p-3 border-2 rounded-lg mb-3"/><datalist id="s-notes">{subjects.map(s=><option key={s}value={s}/>)}</datalist><input type="text"placeholder="Exercice"value={ex}onChange={e=>setEx(e.target.value)}className="w-full p-3 border-2 rounded-lg mb-3"/><input type="text"placeholder="Note"value={score}onChange={e=>setScore(e.target.value)}className="w-full p-3 border-2 rounded-lg mb-3"/><div className="flex gap-2 justify-center mb-3 flex-wrap">{emojis.map(e=><button key={e}onClick={()=>setEmoji(e)}className={`text-4xl p-2 rounded ${emoji===e?'bg-yellow-100':''}`}>{e}</button>)}</div><button onClick={add}className="w-full bg-green-500 text-white p-3 rounded-lg font-bold">Enregistrer</button></div><div className="bg-white rounded-lg shadow p-4 max-h-96 overflow-y-auto"><h3 className="font-bold mb-3">Historique</h3>{Object.entries(notes).length===0?<p className="text-gray-500 text-center py-4">Aucune note</p>:Object.entries(notes).reverse().map(([id,n])=>(<div key={id}className="border rounded-lg p-3 mb-2"><div className="flex justify-between"><div><p className="font-bold">{n.subject}</p><p className="text-sm text-gray-600">{n.exerciseName}</p></div><div className="text-right"><p className="font-bold">{n.score}</p><p className="text-2xl">{n.emoji}</p></div></div></div>))}</div></div>)};const ProgressView=()=>{const stats={};Object.values(calendar).forEach(day=>{Object.entries(day).forEach(([s,d])=>{stats[s]=(stats[s]||0)+d.activities.length})});const total=Object.values(stats).reduce((a,b)=>a+b,0);return(<div className="space-y-4"><div className="bg-gradient-to-r from-blue-500 to-purple-500 text-white rounded-lg shadow-lg p-6"><h3 className="font-bold mb-2 text-xl">📊 Total</h3><p className="text-5xl font-bold">{total}</p><p className="text-lg">Activités</p></div>{total>0&&(<div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">Par matière</h3>{Object.entries(stats).map(([s,c])=>(<div key={s}className="mb-4"><div className="flex justify-between mb-1"><span className="font-semibold">{s}</span><span className="text-gray-600">{c}</span></div><div className="w-full bg-gray-200 rounded-full h-3"><div className="bg-blue-500 h-3 rounded-full"style={{width:`${(c/total)*100}%`}}/></div></div>))}</div>)}</div>)};const OrgView=()=>{const[taskText,setTaskText]=useState('');const[remText,setRemText]=useState('');const[remTime,setRemTime]=useState('');const addTask=()=>{if(taskText.trim()){setTasks([...tasks,{id:Date.now(),text:taskText,done:false}]);setTaskText('')}};const addRem=()=>{if(remText&&remTime){setReminders([...reminders,{id:Date.now(),text:remText,time:remTime}]);setRemText('');setRemTime('')}};const toggle=()=>{if(!audio)return alert('Chargez un audio');isPlaying?audio.pause():audio.play();setIsPlaying(!isPlaying)};return(<div className="space-y-4"><div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">✅ To-Do</h3><div className="flex gap-2 mb-3"><input type="text"placeholder="Tâche"value={taskText}onChange={e=>setTaskText(e.target.value)}onKeyPress={e=>e.key==='Enter'&&addTask()}className="flex-1 p-3 border-2 rounded-lg"/><button onClick={addTask}className="bg-blue-500 text-white px-6 py-3 rounded-lg font-bold">+</button></div>{tasks.map(t=>(<div key={t.id}className="flex items-center gap-2 p-2 border rounded mb-2"><input type="checkbox"checked={t.done}onChange={()=>setTasks(tasks.map(task=>task.id===t.id?{...task,done:!task.done}:task))}className="w-5 h-5"/><span className={t.done?'line-through text-gray-500':''}>{t.text}</span></div>))}</div><div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">🔔 Rappels</h3><input type="text"placeholder="Rappel"value={remText}onChange={e=>setRemText(e.target.value)}className="w-full p-3 border-2 rounded-lg mb-2"/><div className="flex gap-2 mb-3"><input type="datetime-local"value={remTime}onChange={e=>setRemTime(e.target.value)}className="flex-1 p-3 border-2 rounded-lg"/><button onClick={addRem}className="bg-orange-500 text-white px-6 py-3 rounded-lg font-bold">+</button></div>{reminders.map(r=>(<div key={r.id}className="p-3 border-2 rounded-lg bg-orange-50 mb-2"><p className="font-semibold">{r.text}</p><p className="text-sm text-gray-600">{new Date(r.time).toLocaleString('fr-FR')}</p></div>))}</div><div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">🎵 Musique</h3><input type="file"accept="audio/*"onChange={e=>{const f=e.target.files[0];if(f){const url=URL.createObjectURL(f);const a=new Audio(url);a.loop=true;setAudio(a);setIsPlaying(false)}}}className="w-full p-2 border-2 rounded-lg mb-3"/><button onClick={toggle}className={`w-full p-3 rounded-lg text-white font-bold ${!audio?'bg-gray-400':isPlaying?'bg-red-500':'bg-green-500'}`}>{!audio?'⬆️ Charger':isPlaying?'⏸ Pause':'▶ Jouer'}</button></div></div>)};const SettingsView=()=>{const[newSub,setNewSub]=useState('');const[newAct,setNewAct]=useState('');return(<div className="space-y-4"><div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">🎨 Matières</h3><div className="flex gap-2 mb-3"><input type="text"placeholder="Matière"value={newSub}onChange={e=>setNewSub(e.target.value)}className="flex-1 p-3 border-2 rounded-lg"/><button onClick={()=>newSub&&!subjects.includes(newSub)&&(setSubjects([...subjects,newSub]),setNewSub(''))}className="bg-blue-500 text-white px-6 py-3 rounded-lg font-bold">+</button></div>{subjects.map(s=>(<div key={s}className="flex justify-between items-center p-2 border rounded mb-2"><span className="font-semibold">{s}</span><button onClick={()=>setSubjects(subjects.filter(sub=>sub!==s))}className="text-red-500 font-bold">✕</button></div>))}</div><div className="bg-white rounded-lg shadow p-4"><h3 className="font-bold mb-3">📋 Activités</h3><div className="flex gap-2 mb-3"><input type="text"placeholder="Type"value={newAct}onChange={e=>setNewAct(e.target.value)}className="flex-1 p-3 border-2 rounded-lg"/><button onClick={()=>newAct&&!activityTypes.includes(newAct)&&(setActivityTypes([...activityTypes,newAct]),setNewAct(''))}className="bg-green-500 text-white px-6 py-3 rounded-lg font-bold">+</button></div>{activityTypes.map(a=>(<div key={a}className="flex justify-between items-center p-2 border rounded mb-2"><span className="font-semibold">{a}</span><button onClick={()=>setActivityTypes(activityTypes.filter(act=>act!==a))}className="text-red-500 font-bold">✕</button></div>))}</div></div>)};const tabs=[{id:'calendar',label:'Calendrier',component:CalendarView},{id:'notes',label:'Notes',component:NotesView},{id:'progress',label:'Progression',component:ProgressView},{id:'organization',label:'Organisation',component:OrgView},{id:'settings',label:'Paramètres',component:SettingsView}];const C=tabs.find(t=>t.id===tab)?.component;return(<div className="min-h-screen bg-gradient-to-br from-blue-50 to-purple-50"><div className="max-w-4xl mx-auto p-4"><h1 className="text-3xl font-bold text-center mb-6 text-gray-800">📚 Mon App de Révision</h1><div className="flex gap-2 mb-6 overflow-x-auto pb-2">{tabs.map(t=>(<button key={t.id}onClick={()=>setTab(t.id)}className={`px-4 py-2 rounded-lg font-semibold whitespace-nowrap ${tab===t.id?'bg-blue-500 text-white':'bg-white text-gray-700'}`}>{t.label}</button>))}</div>{C&&<C/>}</div></div>)};ReactDOM.render(<App/>,document.getElementById('root'));
</script>
</body>
</html>